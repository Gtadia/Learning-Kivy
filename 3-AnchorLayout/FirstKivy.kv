AnchorLayoutExample:
# 1

<AnchorLayoutExample>:  
    anchor_x: "center"
    anchor_y: "top" # 2
    # Button:
    #     text: "A"
    #     size_hint: (0.1, 0.1) 
    # Button:
    #     text: "B"
    #     size_hint: (0.1, 0.1)     # 3

    BoxLayout:
        size_hint: (0.2, 0.2)       # 4
        Button:
            text: "A"
        Button:
            text: "B"


<BoxLayoutExample>:
    BoxLayout:
        orientation: "vertical" 
        Button:
            text: "A"
            size: "400dp", "200dp"  
            size_hint: 0.8, 0.5 
        Button: 
            text: "B"
            size_hint: None, None 
            size: 100, 150
        Button:
            text: "C"
            size_hint: None, None
            height: "100dp"
            width: "60dp" 
        Label:
            text: "D" 
            size_hint: 0.5, None
            height: "60dp" 
        Button:
            text: "E"
            size_hint: 0.5, None
            height: "60dp" 
            pos_hint: {"x": 0.5, "y": 0} 
        BoxLayout:
            orientation: "horizontal" 
            spacing: "10dp"
            Button:
                text: "1"
            Button: 
                text: "2"
            Button:
                text: "3"


<MainWidget>:
    Button:
        text: "Hello"
        size: 400, 200
        pos: 100, 200

    Button:
        text: "Hello2"
        size: "400dp", "200dp"
        pos: "10dp", "100dp"

    Button:
        text: "Hello 3"
        size: "100dp", "80dp"
        pos: "100dp", "300dp"
        color: 0, 1, 0, 1

    Label:
        text: "Label"
        size: "100dp", "90dp"
        pos: "200dp", "500dp"
        color: 0, 1, 0, 1



# 1 
# If we change the main interface to any of the other custom widgets (EX: BoxLayoutExample and the MainWidget), then we can
# see those respective widgets on the screen.

# 2
# These attributes are specific only to the anchorlayout. 
# There are 3 values that each anchor_x and anchor_y value can have.
# anchor_x      ==>         right, left, center
# anchor_y      ==>         bottom, top, center
# The default value for both x and y is "center"

# 3
# You can see that the "B" button is directly on top of the "A" button. 
# AnchorLayout has no stacking
# If you wnat to position more than one button using AnchorLayout, you can put the buttons in a BoxLayout and put that 
# BoxLayout inside the AnchorLayout

# 4
# when you're adding a layout widget inside of another widget, you can define  that child widget's/layout's size_hint. 